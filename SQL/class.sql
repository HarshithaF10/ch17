create database class;
use class;
create table class_items(id int, classroom_name varchar(8), subjects varchar(24), bench_number int, teacher_name varchar(34), class_time int, phone_number bigint, event_list bigint, programs_scheduled varchar(36));
use class;
desc class_items;
